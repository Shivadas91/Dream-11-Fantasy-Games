kind = "web"
previewPath = "/"
title = "IPL Fantasy League"
version = "1.0.0"
id = "artifacts/ipl-fantasy"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19211

[services.development]
run = "pnpm --filter @workspace/ipl-fantasy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ipl-fantasy", "run", "build" ]
publicDir = "artifacts/ipl-fantasy/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19211"
BASE_PATH = "/"
